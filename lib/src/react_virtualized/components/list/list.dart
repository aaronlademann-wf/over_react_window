part of over_react_virtualized.wrapped_components;

final _List = new ReactJsComponentFactoryProxy(interop.ReactVirtualized.List);

ListDefinition List() => ListDefinition({});
class ListDefinition extends InteropComponentDefinition {
  ListDefinition(Map props) : super(_List, props);
}
